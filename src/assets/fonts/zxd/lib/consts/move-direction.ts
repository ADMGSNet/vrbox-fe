export enum ZDatagridMoveDirection {
  Up = 'up',
  Down = 'down',
  Left = 'left',
  Right = 'right',
}

/**
 * Move direction for popups (z-combobox-popup, z-popup-menu)
 */
export enum ZPopupMoveDirection {
  Up = 'up',
  Down = 'down',
}

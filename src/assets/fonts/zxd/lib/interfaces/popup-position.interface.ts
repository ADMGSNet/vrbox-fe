export interface PopupPosition {
  /**
   * Top position (in string)
   */
  top: string;

  /**
   * Left position (in px)
   */
  x: number;

  /**
   * Bottom position (in string)
   */
  bottom: string;
}
